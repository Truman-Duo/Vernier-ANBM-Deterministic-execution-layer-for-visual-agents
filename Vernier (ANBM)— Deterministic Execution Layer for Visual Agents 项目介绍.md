
# Vernier — Deterministic Execution Layer for Visual Agents 项目介绍

内部代号 `anbm`。

## 一句话定义

为视觉 Agent 提供**多步任务的状态记忆与确定性路径**，让视觉模型只处理关键决策，常规执行走结构化通路。

---

## 解决的问题

现有的视觉 Agent（OpenAI Operator、Claude Computer Use 等）在**单步**操作上已足够可靠，但在**多步任务链**中存在两个系统性问题：

1. **无状态导致的错误累积**  
   每一步都重新截图、重新理解整个页面，没有对上一步操作结果的记忆。一次误解可能导致后续步骤全部偏移，且无法回溯。

2. **冗余的 Token 消耗**  
   即使页面无变化（如翻页、提取固定列表），每一步仍需编码图像、调用视觉模型，产生大量确定性的成本浪费。

**ANBM 不是替代视觉方案，而是为它提供两个补充能力：**  
- 记住“多步之间发生了什么”  
- 在确定性步骤上跳过视觉，直接返回结构化数据

---

## 核心设计

### 1. 状态机引擎

为每个目标网站/任务域定义一个有限状态机。  
例如 GitHub Issue 管理：

```
未登录 → 已登录 → Issue 列表 → 新建表单 → 提交成功
```

每个状态附带：
- **校验条件**：URL 模式、关键 DOM 元素存在性、Schema 结构
- **预期的允许操作**：当前状态下 Agent 可以执行的动作

操作执行后，ANBM 自动校验新状态是否与预期一致。  
若校验失败，立即向 Agent 报告异常，避免在错误路径上继续执行。

### 2. 混合执行策略

| 步骤类型 | 执行方式 | 说明 |
|---------|--------|------|
| 已在 Adapter 中建模的确定性操作 | 通过 Adapter 提取结构化数据 | 翻页、提取列表、获取状态字段等，不走视觉 |
| 页面出现未建模元素、弹窗、异常布局 | 回退到视觉模型处理 | 视觉模型负责“看懂”未知成分 |
| 关键决策点（需语义判断） | 调视觉模型 | 例如判断某条 issue 是否包含特定信息 |

通过这种混合策略，视觉模型的调用次数可降低 60-80%，而关键决策仍保留视觉兜底。

### 3. Adapter 模式

每个网站的状态机、Schema 和提取/操作逻辑封装为一个 Adapter（两个文件）：

- **manifest.json**：URL 匹配模式、输出 Schema 定义、状态转换图、操作幂等性申明
- **handler.py**：实现 `extract`（提取）和 `act`（执行）方法

Adapter 由社区开发和维护。ANBM 只提供框架和运行时，不承诺自动适应网站改版。

### 4. 状态敏感的重试机制

ANBM 在确定性路径中内建了**分级重试**策略，让瞬态故障（页面加载延迟）和结构性变化（状态跳转）被分别对待：

- **重试的前提是“状态未变”**：每次重试前都会重新检测当前页面状态，只有确认页面仍处于预期状态时才会继续重试，避免在状态已经跳转（例如突然弹出登录窗口）时仍盲目执行，掩盖掉真正的路径变化。
- **“无法判定”不等于“状态跳转”**：当页面处于过渡动画或加载中导致状态检测暂时无法判定时，系统会将其视为不稳定的观测噪声，继续重试而不是误判为跳转。只有在检测到明确的、不同的已知状态时，才会终止重试并将控制权交还给状态机进行重新规划。
- **非幂等操作零重试**：对于发表评论、提交表单等会产生重复副作用的写操作，系统**完全不进行重试**，失败后直接向 Agent 报告并等待人工决策，从根本上杜绝重复提交。

这三条策略让 ANBM 的执行路径在“稳定 → 噪声 → 异常”三种情况下都保持可预期，而不是一个黑盒重试循环。

---

## 与视觉方案的关系

**不是竞争，是分层协作。**

- **视觉方案** 负责感知层：识别元素、理解复杂布局、处理未知页面
- **ANBM** 负责执行层：维护任务状态、校验步骤、在确定性路径上返回结构化结果，并在状态变化时及时上报

类比：  
视觉方案 = 人眼和大脑的实时判断  
ANBM = 固定路线上的轨道和信号系统，让车不用每米都重新认路，同时能察觉岔路并发出告警

---

## 技术架构

```
AI Agent (视觉模型)
      │
      │ 关键决策调视觉
      │ 确定性路径走结构化
      ▼
ANBM 状态机引擎
      ├── 状态校验（含状态变化检测、unknown容错）
      ├── 分级重试编排（只在状态未变时重试）
      ├── Adapter 加载
      └── 结构化数据/操作指令 → 失败时视觉兜底
      │
      ▼
Headless 浏览器 (Playwright)
```

协议层提供 REST API（`/browse`, `/act`, `/session`），后续兼容 MCP，可直接嵌入现有 Agent 框架。

---

## 开发路线图

| 阶段 | 目标 | 交付物 |
|------|------|--------|
| 第一阶段 | 豆瓣电影 Top250（只读） | Adapter + 完整重试与状态跳转处理 + API |
| 第二阶段 | Reddit 子版块（登录+投票） | 多步状态机 + 非幂等安全保障 + 会话持久化 |
| 第三阶段 | GitHub Issues（完整工作流） | 混合视觉调用的完整闭环 |

每个阶段都交付一个可用的服务，而不是一个未来承诺。

---

## 项目定位与边界

**ANBM 是**  
- 一个开源的状态机执行框架，外加一组社区维护的网站 Adapter
- 一种降低 Agent 推理成本、提高多步任务可靠性的工程工具
- 一个在确定性路径上提供智能重试、在状态跳转时主动告警的中间层

**ANBM 不是**  
- 一个自动适应所有网站变化的万能层
- 一个能替代视觉模型理解网页的 AI 系统
- 一个反爬虫对抗方案

如果网站后端大规模改版，对应的 Adapter 需要开发者更新。ANBM 只保证：当 Adapter 有效时，执行是确定性的；当 Adapter 失效时，系统会明确报错并通知 Agent，而不是静默产生错误结果。

---

## 为什么是现在

Agent 的调用量和任务复杂度正在快速增长。视觉模型的单步能力已经足够好，但多步任务的工程可靠性仍是空白。这层补缺工作不依赖任何一方（大模型厂商、浏览器厂商、网站方）的配合，技术上在开源社区的能力范围内，且对大公司无商业利益——这正是一个社区驱动的中间层最合适的起点。